
@model IEnumerable<InventoryManagementSystem.Models.Supplier>

@{
    ViewData["Title"] = "Supplier List";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h3>All Products List</h3>
    <div class="d-flex">
        @* -- Search Box -- *@
        <form asp-action="Index" method="get" class="d-flex me-2">
            <input type="text" name="searchText" value="@ViewData["CurrentFilter"]" class="form-control me-2" placeholder="Search by Name" />
            <button type="submit" class="btn btn-info">Search</button>
        </form>

        <a asp-action="Create" class="btn btn-info">Add New Product</a>
    </div>
</div>

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Country</th>
            <th>ContactInfo</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            foreach (var supplier in Model)
            {
                <tr>
                    <td>@supplier.Id</td>
                    <td>@supplier.Name</td>
                    <td>@supplier.Country</td>
                    <td>@supplier.ContactInfo</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@supplier.Id" class="btn btn-warning btn-sm">Edit</a>

                        @* -- Delete Modal --  *@
                        <button type="button"
                                class="btn btn-danger btn-sm"
                                data-bs-toggle="modal"
                                data-bs-target="#deleteModal-@supplier.Id">
                            Delete
                        </button>

                        @*  Delete Confirmation Modal  *@
                        <div class="modal fade" id="deleteModal-@supplier.Id" tabindex="-1">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <form asp-action="Delete" method="post" asp-route-id="@supplier.Id">
                                        <div class="modal-header">
                                            <h5 class="modal-title">Confirm Delete</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                        </div>
                                        <div class="modal-body">
                                            Are you sure you want to delete <strong>@supplier.Name</strong>?
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                            <button type="submit" class="btn btn-danger">Yes, Delete</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                        @* -- End Delete Modal -- *@
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="8" class="text-center">No suppiers available</td>
            </tr>
        }
    </tbody>
</table>
